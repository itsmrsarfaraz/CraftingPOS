<UserControl x:Class="CraftingPOS.Presentation.Views.UsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <UserControl.Resources>
        <Style x:Key="PanelStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="18"/>
        </Style>
        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="Gray"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="#F3F4F6">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="16"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="16"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="1*" MinWidth="320"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3"
                       Text="User Management" FontSize="24" FontWeight="Bold" Foreground="#1E3A8A"/>

            <!-- Discount Limits (Owner/SystemAdmin) -->
            <Border Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" Style="{StaticResource PanelStyle}">
                <StackPanel>
                    <TextBlock Text="Cashier Discount Limits" FontWeight="Bold" FontSize="15" Margin="0,0,0,10"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Max Percentage (%)" Style="{StaticResource FieldLabel}"/>
                            <TextBox Text="{Binding MaxCashierDiscountPercent, UpdateSourceTrigger=PropertyChanged}" Width="140" Padding="6"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Max Flat Amount (Rs.)" Style="{StaticResource FieldLabel}"/>
                            <TextBox Text="{Binding MaxCashierDiscountFlat, UpdateSourceTrigger=PropertyChanged}" Width="140" Padding="6"/>
                        </StackPanel>
                        <Button Grid.Column="4" Content="Save Limits" VerticalAlignment="Bottom" Height="32" Padding="16,0"
                                Background="#1E3A8A" Foreground="White" FontWeight="Bold"
                                Command="{Binding SaveLimitsCommand}"/>
                    </Grid>
                    <TextBlock Text="{Binding LimitsStatusMessage}" FontSize="12" Margin="0,8,0,0">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="#16A34A"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding LimitsHasError}" Value="True">
                                        <Setter Property="Foreground" Value="#DC2626"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </StackPanel>
            </Border>

            <!-- Users list -->
            <Border Grid.Row="4" Grid.Column="0" Style="{StaticResource PanelStyle}">
                <DataGrid ItemsSource="{Binding Users}"
                          AutoGenerateColumns="False" IsReadOnly="True"
                          GridLinesVisibility="Horizontal" HeadersVisibility="Column"
                          RowHeight="34" CanUserAddRows="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="*"/>
                        <DataGridTextColumn Header="Full Name" Binding="{Binding FullName}" Width="*"/>
                        <DataGridTextColumn Header="Role" Binding="{Binding RoleName}" Width="Auto"/>
                        <DataGridTextColumn Header="Last Login" Binding="{Binding LastLoginAt, StringFormat='dd MMM yyyy'}" Width="Auto"/>
                        <DataGridTemplateColumn Header="" Width="180">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="Reset PW" FontSize="11" Padding="6,2" Margin="0,0,6,0"
                                                Background="#DBEAFE" Foreground="#1E3A8A" BorderThickness="0"
                                                Command="{Binding DataContext.ResetPasswordCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"/>
                                        <Button Content="Deactivate" FontSize="11" Padding="6,2"
                                                Background="#FEE2E2" Foreground="#B91C1C" BorderThickness="0"
                                                Command="{Binding DataContext.DeactivateUserCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <!-- Create user form -->
            <Border Grid.Row="4" Grid.Column="2" Style="{StaticResource PanelStyle}" VerticalAlignment="Top">
                <StackPanel>
                    <TextBlock Text="Create Account" FontWeight="Bold" FontSize="15" Margin="0,0,0,14"/>

                    <TextBlock Text="Username *" Style="{StaticResource FieldLabel}"/>
                    <TextBox Text="{Binding NewUsername, UpdateSourceTrigger=PropertyChanged}" Padding="6" Margin="0,0,0,10"/>

                    <TextBlock Text="Full Name *" Style="{StaticResource FieldLabel}"/>
                    <TextBox Text="{Binding NewFullName, UpdateSourceTrigger=PropertyChanged}" Padding="6" Margin="0,0,0,10"/>

                    <TextBlock Text="Temporary Password *" Style="{StaticResource FieldLabel}"/>
                    <TextBox Text="{Binding NewPassword, UpdateSourceTrigger=PropertyChanged}" Padding="6" Margin="0,0,0,10"/>

                    <TextBlock Text="Role *" Style="{StaticResource FieldLabel}"/>
                    <ComboBox ItemsSource="{Binding AssignableRoles}" SelectedItem="{Binding SelectedRole}"
                              Padding="6" Margin="0,0,0,12"/>

                    <TextBlock Text="{Binding StatusMessage}" FontSize="12" TextWrapping="Wrap" Margin="0,0,0,10">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="#16A34A"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding HasError}" Value="True">
                                        <Setter Property="Foreground" Value="#DC2626"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <Button Content="Create Account" Height="34" FontWeight="Bold"
                            Background="#16A34A" Foreground="White"
                            Command="{Binding CreateUserCommand}"/>
                </StackPanel>
            </Border>
        </Grid>

        <Border Background="#AAFFFFFF" Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}">
            <TextBlock Text="Working..." HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16"/>
        </Border>
    </Grid>
</UserControl>