<UserControl x:Class="CraftingPOS.Presentation.Views.LicenseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <UserControl.Resources>
        <Style x:Key="PanelStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="20"/>
        </Style>
        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="Gray"/>
        </Style>
        <Style x:Key="FieldValue" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,2,0,14"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="#F3F4F6">
        <StackPanel Margin="24" MaxWidth="500" HorizontalAlignment="Left">
            <TextBlock Text="License" FontSize="24" FontWeight="Bold" Foreground="#1E3A8A" Margin="0,0,0,16"/>

            <Border Style="{StaticResource PanelStyle}">
                <StackPanel>
                    <Grid Margin="0,0,0,4">
                        <TextBlock Text="Status" FontWeight="Bold" FontSize="15"/>
                        <Border HorizontalAlignment="Right" CornerRadius="10" Padding="10,3">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="#DCFCE7"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsValid}" Value="False">
                                            <Setter Property="Background" Value="#FEE2E2"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <TextBlock FontSize="11" FontWeight="Bold">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="ACTIVE"/>
                                        <Setter Property="Foreground" Value="#166534"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsValid}" Value="False">
                                                <Setter Property="Text" Value="INVALID"/>
                                                <Setter Property="Foreground" Value="#B91C1C"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Border>
                    </Grid>

                    <Separator Margin="0,10,0,16"/>

                    <TextBlock Text="Business Name" Style="{StaticResource FieldLabel}"/>
                    <TextBlock Text="{Binding BusinessName}" Style="{StaticResource FieldValue}"/>

                    <TextBlock Text="License Type" Style="{StaticResource FieldLabel}"/>
                    <TextBlock Text="{Binding LicenseType}" Style="{StaticResource FieldValue}"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Activation Date" Style="{StaticResource FieldLabel}"/>
                            <TextBlock Text="{Binding IssuedAt}" Style="{StaticResource FieldValue}"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Expiry Date" Style="{StaticResource FieldLabel}"/>
                            <TextBlock Text="{Binding ExpiresAt}" Style="{StaticResource FieldValue}"/>
                        </StackPanel>
                    </Grid>

                    <TextBlock Text="Machine ID" Style="{StaticResource FieldLabel}"/>
                    <Grid Margin="0,2,0,0">
                        <TextBlock Text="{Binding MachineFingerprint}" FontFamily="Consolas" FontSize="13" VerticalAlignment="Center"/>
                        <Button Content="Copy" HorizontalAlignment="Right" FontSize="11" Padding="8,2"
                                Background="#E5E7EB"
                                Command="{Binding CopyFingerprintCommand}"/>
                    </Grid>

                    <TextBlock Text="{Binding StatusMessage}" TextWrapping="Wrap" FontSize="12" Margin="0,14,0,10">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="#16A34A"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding HasError}" Value="True">
                                        <Setter Property="Foreground" Value="#DC2626"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <Button Content="Import / Renew License..." Height="36" FontWeight="Bold"
                            Background="#1E3A8A" Foreground="White"
                            Command="{Binding ImportLicenseCommand}"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>