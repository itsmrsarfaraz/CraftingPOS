<UserControl x:Class="CraftingPOS.Presentation.Views.AuditLogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <UserControl.Resources>
        <Style x:Key="PanelStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="18"/>
        </Style>
        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="Gray"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="#F3F4F6">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="16"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="16"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Audit Logs" FontSize="24" FontWeight="Bold" Foreground="#1E3A8A"/>

            <!-- Filters -->
            <Border Grid.Row="2" Style="{StaticResource PanelStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="14"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="14"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="14"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="14"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Module" Style="{StaticResource FieldLabel}"/>
                        <ComboBox ItemsSource="{Binding Modules}" SelectedItem="{Binding SelectedModule}" Width="140" Padding="6"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Username" Style="{StaticResource FieldLabel}"/>
                        <TextBox Text="{Binding UsernameFilter, UpdateSourceTrigger=PropertyChanged}" Width="120" Padding="6"/>
                    </StackPanel>

                    <StackPanel Grid.Column="4">
                        <TextBlock Text="Keyword" Style="{StaticResource FieldLabel}"/>
                        <TextBox Text="{Binding KeywordFilter, UpdateSourceTrigger=PropertyChanged}" Width="160" Padding="6"/>
                    </StackPanel>

                    <StackPanel Grid.Column="6">
                        <TextBlock Text="From" Style="{StaticResource FieldLabel}"/>
                        <DatePicker SelectedDate="{Binding FromDate}" Width="130" Padding="6"/>
                    </StackPanel>

                    <StackPanel Grid.Column="8">
                        <TextBlock Text="To" Style="{StaticResource FieldLabel}"/>
                        <DatePicker SelectedDate="{Binding ToDate}" Width="130" Padding="6"/>
                    </StackPanel>

                    <Button Grid.Column="10" Content="Search" Height="32" Padding="14,0" Margin="0,18,8,0"
                            Background="#1E3A8A" Foreground="White" FontWeight="Bold"
                            Command="{Binding SearchCommand}"/>

                    <Button Grid.Column="11" Content="Clear" Height="32" Padding="14,0" Margin="0,18,0,0"
                            Background="#E5E7EB"
                            Command="{Binding ClearFiltersCommand}"/>
                </Grid>
            </Border>

            <!-- Results -->
            <Border Grid.Row="4" Style="{StaticResource PanelStyle}">
                <DataGrid ItemsSource="{Binding Logs}"
                          AutoGenerateColumns="False" IsReadOnly="True"
                          GridLinesVisibility="Horizontal" HeadersVisibility="Column"
                          RowHeight="32" CanUserAddRows="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Date/Time" Binding="{Binding CreatedAt, StringFormat='dd MMM yyyy HH:mm:ss'}" Width="Auto"/>
                        <DataGridTextColumn Header="User" Binding="{Binding Username}" Width="Auto"/>
                        <DataGridTextColumn Header="Module" Binding="{Binding Module}" Width="Auto"/>
                        <DataGridTextColumn Header="Action" Binding="{Binding Action}" Width="Auto"/>
                        <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>

        <Border Background="#AAFFFFFF" Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}">
            <TextBlock Text="Loading..." HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16"/>
        </Border>
    </Grid>
</UserControl>