<Window x:Class="CraftingPOS.Presentation.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CraftingPOS - Login"
        Height="440" Width="380"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="24"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="6"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="14"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="6"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="CraftingPOS"
                   FontSize="28" FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Foreground="#1E3A8A"/>

        <TextBlock Grid.Row="1" Text="Point of Sale System"
                   FontSize="12" Foreground="Gray"
                   HorizontalAlignment="Center" Margin="0,4,0,0"/>

        <TextBlock Grid.Row="3" Text="Username"/>
        <TextBox Grid.Row="4" x:Name="UsernameBox"
                 Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                 Padding="6" FontSize="14"/>

        <TextBlock Grid.Row="6" Text="Password"/>
        <PasswordBox Grid.Row="7" x:Name="PasswordBox"
                     Padding="6" FontSize="14"
                     KeyDown="PasswordBox_KeyDown"/>

        <TextBlock Grid.Row="9" Text="{Binding ErrorMessage}"
                   Foreground="Red" FontSize="12"
                   TextWrapping="Wrap"
                   Visibility="{Binding HasError, Converter={StaticResource BoolToVis}}"/>

        <Button Grid.Row="11" Content="Login"
                Height="36" FontSize="14" FontWeight="Bold"
                Background="#1E3A8A" Foreground="White"
                Click="LoginButton_Click"/>

        <Button Grid.Row="13" Content="Exit"
                Height="28" FontSize="12"
                Background="Transparent" BorderThickness="0"
                Foreground="Gray"
                Command="{Binding ExitCommand}"/>

        <TextBlock Grid.Row="15" Text="v1.0.0"
                   FontSize="10" Foreground="Gray"
                   HorizontalAlignment="Center"/>
    </Grid>
</Window>