<Window x:Class="CraftingPOS.Presentation.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CraftingPOS" Height="700" Width="1200"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized">

	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="220"/>
			<ColumnDefinition Width="*"/>
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="56"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<!-- Top Header -->
		<Border Grid.Row="0" Grid.ColumnSpan="2" Background="#1E3A8A">
			<Grid Margin="20,0">
				<TextBlock Text="CraftingPOS" Foreground="White" FontSize="18" FontWeight="Bold"
                           VerticalAlignment="Center" HorizontalAlignment="Left"/>
				<TextBlock Text="{Binding WelcomeMessage}" Foreground="White" FontSize="13"
                           VerticalAlignment="Center" HorizontalAlignment="Right"/>
			</Grid>
		</Border>

		<!-- Sidebar -->
		<Border Grid.Row="1" Grid.Column="0" Background="#111827">
			<StackPanel Margin="0,16,0,0">
				<Button Content="Dashboard" Style="{StaticResource SidebarButtonStyle}"/>
				<Button Content="Sales" Style="{StaticResource SidebarButtonStyle}" IsEnabled="False"/>
				<Button Content="Products" Style="{StaticResource SidebarButtonStyle}" IsEnabled="False"/>
				<Button Content="Categories" Style="{StaticResource SidebarButtonStyle}" IsEnabled="False"/>
				<Button Content="Inventory" Style="{StaticResource SidebarButtonStyle}" IsEnabled="False"/>
				<Button Content="Purchases" Style="{StaticResource SidebarButtonStyle}" IsEnabled="False"/>
				<Button Content="Customers" Style="{StaticResource SidebarButtonStyle}" IsEnabled="False"/>
				<Button Content="Suppliers" Style="{StaticResource SidebarButtonStyle}" IsEnabled="False"/>
				<Button Content="Reports" Style="{StaticResource SidebarButtonStyle}" IsEnabled="False"/>
				<Button Content="Settings" Style="{StaticResource SidebarButtonStyle}" IsEnabled="False"/>
			</StackPanel>
		</Border>

		<!-- Main Content -->
		<ContentControl Grid.Row="1" Grid.Column="1" x:Name="MainContent"/>
	</Grid>

	<Window.Resources>
		<Style x:Key="SidebarButtonStyle" TargetType="Button">
			<Setter Property="HorizontalContentAlignment" Value="Left"/>
			<Setter Property="Padding" Value="20,12"/>
			<Setter Property="Background" Value="Transparent"/>
			<Setter Property="Foreground" Value="White"/>
			<Setter Property="BorderThickness" Value="0"/>
			<Setter Property="FontSize" Value="14"/>
			<Setter Property="Cursor" Value="Hand"/>
			<Style.Triggers>
				<Trigger Property="IsEnabled" Value="False">
					<Setter Property="Foreground" Value="#6B7280"/>
				</Trigger>
				<Trigger Property="IsMouseOver" Value="True">
					<Setter Property="Background" Value="#1F2937"/>
				</Trigger>
			</Style.Triggers>
		</Style>
	</Window.Resources>
</Window>